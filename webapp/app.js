const tg=window.Telegram?.WebApp;tg?.ready();tg?.expand();tg?.setHeaderColor?.('#07111f');tg?.setBackgroundColor?.('#07111f');const initData=tg?.initData||'',preview=!initData,H={'X-Telegram-Init-Data':initData,'Content-Type':'application/json'},$=id=>document.getElementById(id),q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const money=n=>new Intl.NumberFormat('fa-IR').format(Number(n||0))+' ریال';const size=n=>{let v=Number(n||0),i=0,u=['B','KB','MB','GB','TB','PB'];while(v>=1024&&i<u.length-1){v/=1024;i++}return new Intl.NumberFormat('fa-IR',{maximumFractionDigits:i>2?2:0}).format(v)+' '+u[i]};const date=v=>v?new Intl.DateTimeFormat('fa-IR',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v)):'—';let state={caps:null,dashboard:null,mode:'',resellers:[],panelId:'',panelName:'',nodeName:'',userOffset:0};async function api(path,options={}){const response=await fetch(path,{...options,headers:{...H,...options.headers}});let body={};try{body=await response.json()}catch{}if(!response.ok)throw Error(body.detail||`خطای ارتباط (${response.status})`);return body}function toast(message,bad=false){const el=$('toast');el.textContent=message;el.style.borderColor=bad?'#7d3045':'#385273';el.classList.remove('hidden');setTimeout(()=>el.classList.add('hidden'),3000)}function loading(id,count=2){$(id).innerHTML=Array(count).fill('<div class="skeleton"></div>').join('')}function card(title,subtitle,actions=''){return `<article class="data-card"><div class="main"><h3>${esc(title)}</h3><p>${subtitle}</p></div><div class="card-actions">${actions}</div></article>`}function roleFa(role){return({reseller_admin:'مدیر نمایندگی',operator:'اپراتور',finance:'مدیر مالی',support:'پشتیبان',viewer:'مشاهده‌گر'})[role]||role||'کاربر'}function setPage(id){qa('.page,.side-nav button,.bottom-nav button').forEach(el=>el.classList.remove('active'));$(id).classList.add('active');qa(`[data-page="${id}"]`).forEach(el=>el.classList.add('active'));const titles={dashboard:'داشبورد',network:state.caps?.is_system_admin?'مدیریت نمایندگان':'شبکه فروش',servers:'سرورها و نودها',finance:'امور مالی',account:'حساب و پشتیبانی'};$('page-title').textContent=titles[id];if(id==='network')loadResellers();if(id==='servers')loadServers();if(id==='finance')loadFinance();if(id==='account')renderProfile();tg?.HapticFeedback?.selectionChanged()}const perm=name=>!!state.caps?.permissions?.[name];function applyCapabilities(){const admin=!!state.caps?.is_system_admin;qa('.admin-only').forEach(el=>el.classList.toggle('hidden',!admin));qa('[data-permission]').forEach(el=>el.classList.toggle('hidden',!perm(el.dataset.permission)));$('role-label').textContent=admin?'مدیر کل سیستم':roleFa(state.caps?.role);$('network-title').textContent=admin?'مدیریت نمایندگان':'زیرمجموعه‌های من';$('pending-section').classList.toggle('hidden',!perm('decide_funding'));$('approvals-section').classList.toggle('hidden',!perm('decide_adjustment'))}async function boot(){if(preview){demo();return}try{const session=await api('/v1/webapp/session');if(session.needs_setup){$('setup').classList.remove('hidden');return}state.caps=await api('/v1/webapp/capabilities');applyCapabilities();await loadDashboard();$('status-pill').textContent='آنلاین';$('connection-label').textContent='اتصال امن برقرار است'}catch(error){$('status-pill').textContent='خطا';$('status-pill').classList.add('error');toast(error.message,true)}}function demo(){state.caps={is_system_admin:true,role:'system_admin',permissions:Object.fromEntries(['view_dashboard','view_finance','view_audit','export_reports','create_support_ticket','answer_support','request_funding','decide_funding','request_adjustment','decide_adjustment','manage_servers','node_control','subscriber_control','admin_limit','change_billing_coefficient','manage_resellers'].map(name=>[name,true]))};state.dashboard={organization:{name:'نمایندگی مرکزی'},actor:{name:'مدیر سیستم',role:'reseller_admin'},wallet:{available_irr:185000000,balance_irr:135000000,credit_limit_irr:50000000},usage:{lifetime_bytes:3092376453120},children_count:12};applyCapabilities();renderDashboard();$('status-pill').textContent='پیش‌نمایش'}async function loadDashboard(){state.dashboard=await api('/v1/webapp/dashboard');renderDashboard();if(state.caps.is_system_admin){const overview=await api('/v1/webapp/admin/overview');renderAdminKpis(overview)}}function renderDashboard(){const d=state.dashboard,org=d.organization.name;$('org-name').textContent=org;$('profile-org').textContent=org;$('avatar').textContent=org.trim()[0]||'P';$('profile-avatar').textContent=org.trim()[0]||'P';$('welcome').textContent=`${d.actor.name||'کاربر'} · ${roleFa(d.actor.role)}`;$('available').textContent=money(d.wallet.available_irr);$('balance').textContent=money(d.wallet.balance_irr);$('credit').textContent=money(d.wallet.credit_limit_irr);$('usage').textContent=size(d.usage.lifetime_bytes);$('usage-time').textContent=d.usage.observed_at?date(d.usage.observed_at):'هنوز همگام نشده';$('children-count').textContent=new Intl.NumberFormat('fa-IR').format(d.children_count);$('network-badge').textContent=d.children_count}function renderAdminKpis(o){const el=$('admin-kpis');el.classList.remove('hidden');el.innerHTML=[['کل سازمان‌ها',o.organizations],['سرور فعال',o.panels],['کاربر متصل',o.actors],['درخواست باز',o.pending_funding]].map(([x,v])=>`<article><b>${new Intl.NumberFormat('fa-IR').format(v)}</b><span>${x}</span></article>`).join('');$('fund-badge').textContent=o.pending_funding;$('fund-badge').classList.toggle('hidden',!o.pending_funding)}async function loadResellers(){loading('reseller-list',3);try{const rows=preview?[{name:'نماینده شمال',slug:'north',status:'active',telegram_id:123456789,balance_irr:42000000,credit_limit_irr:10000000,price_per_gib_irr:18500}]:await api(state.caps.is_system_admin?'/v1/webapp/admin/resellers':'/v1/webapp/children');state.resellers=rows;renderResellers(rows)}catch(error){$('reseller-list').innerHTML=`<div class="notice">${esc(error.message)}</div>`}}function renderResellers(rows){if(!rows.length){$('reseller-list').innerHTML='<div class="notice"><span>👥</span><div><b>هنوز نماینده‌ای ثبت نشده</b><p>اولین نماینده یا زیرمجموعه فروش خود را ایجاد کنید.</p></div></div>';return}$('reseller-list').innerHTML=rows.map(x=>{const total=Number(x.balance_irr||0)+Number(x.credit_limit_irr||0),meta=state.caps.is_system_admin?`${esc(x.slug||'')} · تلگرام: ${esc(x.telegram_id||'متصل نشده')} · قیمت گیگ: ${money(x.price_per_gib_irr||0)}`:`اعتبار قابل استفاده: ${money(total)}`;return card(x.name,meta,`<span class="badge ${x.status==='active'?'':'off'}">${x.status==='active'?'فعال':'غیرفعال'}</span><b>${money(total)}</b>`)}).join('')}async function loadServers(){loading('server-list',2);try{const rows=preview?[{id:'demo',name:'سرور اصلی پاسارگارد',base_url:'https://panel.example.com',status:'active'}]:await api('/v1/webapp/panels');const actions=x=>[`<span class="badge ${x.status==='active'?'':'off'}">${x.status==='active'?'متصل':'قطع'}</span>`,perm('manage_servers')?`<button class="ghost" data-panel="${x.id}" data-name="${esc(x.name)}">مشاهده نودها</button>`:'',perm('admin_limit')?`<button class="ghost" data-tool="limit" data-id="${x.id}" data-name="${esc(x.name)}">سقف پنل</button>`:'',perm('change_billing_coefficient')?`<button class="ghost" data-tool="coefficient" data-id="${x.id}" data-name="${esc(x.name)}">ضریب صورتحساب</button>`:''].join('');$('server-list').innerHTML=rows.length?rows.map(x=>card(x.name,esc(x.base_url),actions(x))).join(''):'<div class="notice"><span>🖥</span><div><b>سروری ثبت نشده</b><p>برای اتصال به PasarGuard اولین سرور را ثبت کنید.</p></div></div>';const select=btn=>{state.panelId=btn.dataset.panel||btn.dataset.id;state.panelName=btn.dataset.name};qa('[data-panel]').forEach(btn=>btn.onclick=()=>{select(btn);state.userOffset=0;$('user-panel').classList.remove('hidden');$('users-title').textContent=`مشترکان ${btn.dataset.name}`;loadNodes(btn.dataset.panel,btn.dataset.name);loadUsers()});qa('[data-tool]').forEach(btn=>btn.onclick=()=>{select(btn);openModal(btn.dataset.tool)})}catch(error){$('server-list').innerHTML=`<div class="notice">${esc(error.message)}</div>`}}const NODE_ON=n=>n.enable===true||['online','active','connected'].includes(String(n.status||''));const nodeState=n=>NODE_ON(n)?'on':'off';
async function runNodeOp(panelId,nodeId,kind,button){button.disabled=true;try{if(kind==='status'){const result=await api(`/v1/webapp/panels/${panelId}/nodes/${nodeId}/status`);toast(`وضعیت: ${String(result&&result.message||JSON.stringify(result)).slice(0,120)}`)}else{await api(`/v1/webapp/panels/${panelId}/nodes/${nodeId}/${kind}`,{method:'POST'});toast(NODE_RESULT[kind]);await loadNodes(panelId,state.nodeName)}}catch(error){toast(error.message,true)}finally{button.disabled=false}}
const NODE_RESULT={reconnect:'دستور اتصال مجدد ارسال شد',enable:'نود روشن شد',disable:'نود خاموش شد',reset:'دستور بازنشانی نود ارسال شد'};
function nodeRow(panelId,n){const on=NODE_ON(n),base=`/v1/webapp/panels/${panelId}/nodes/${n.id}`;const controls=perm('node_control')?`<button class="ghost" data-node="${base}/reconnect">اتصال مجدد</button><button class="ghost" data-node="${base}/${on?'disable':'enable'}">${on?'خاموش کردن':'روشن کردن'}</button><button class="ghost danger" data-node="${base}/reset">بازنشانی</button>`:'';return card(n.name||`Node ${n.id}`,esc(n.address||n.message||n.status||'نود پاسارگارد'),`<span class="badge ${on?'':'off'}">${on?'فعال':'خاموش'}</span>${controls}<button class="ghost" data-node="${base}/status">وضعیت</button>`)}
async function loadNodes(panelId,name){state.nodeName=name;$('node-panel').classList.remove('hidden');$('node-title').textContent=`نودهای ${name}`;loading('node-list',2);try{const result=preview?{nodes:[{id:1,name:'Germany-01',status:'connected'},{id:2,name:'Finland-02',enable:false}]}:await api(`/v1/webapp/panels/${panelId}/nodes`);const nodes=result.nodes||[];$('node-list').innerHTML=nodes.length?nodes.map(n=>nodeRow(panelId,n)).join(''):'<div class="notice">نودی از پنل دریافت نشد.</div>';qa('[data-node]').forEach(btn=>btn.onclick=()=>runNodeOp(panelId,btn.dataset.node.split('/').at(-2),btn.dataset.node.split('/').pop(),btn))}catch(error){$('node-list').innerHTML=`<div class="notice">${esc(error.message)}</div>`}}
async function loadUsers(){const panelId=state.panelId;if(!panelId){toast('ابتدا یک سرور را انتخاب کنید',true);return}if(!perm('subscriber_control')){$('user-panel').classList.add('hidden');return}const controls=u=>`<button class="ghost" data-user="/v1/webapp/panels/${panelId}/users/${u.id}/${u.status===false?'enable':'disable'}">${u.status===false?'فعال کردن':'غیرفعال کردن'}</button><button class="ghost danger" data-user="/v1/webapp/panels/${panelId}/users/${u.id}/reset-data">پاک کردن مصرف</button>`;loading('user-list',2);try{const result=preview?{users:[{id:11,username:'subscriber-a',status:true,used_traffic:858993459}],total:1}:await api(`/v1/webapp/panels/${panelId}/users?offset=${state.userOffset}&limit=20`);const users=result.users||[];$('user-count').textContent=new Intl.NumberFormat('fa-IR').format(result.total??users.length);$('user-list').innerHTML=users.length?users.map(u=>card(u.username||`کاربر ${u.id}`,`${size(u.used_traffic||u.lifetime_used_traffic||0)} · شناسه ${esc(u.id)}`,`<span class="badge ${u.status===false?'off':''}">${u.status===false?'غیرفعال':'فعال'}</span>${controls(u)}`)).join(''):'<div class="notice">مشترکی برای این سرور ثبت نشده است.</div>';qa('[data-user]').forEach(btn=>btn.onclick=async()=>{btn.disabled=true;try{await api(btn.dataset.user,{method:'POST'});toast('دستور به پنل ارسال شد');await loadUsers()}catch(error){toast(error.message,true)}finally{btn.disabled=false}})}catch(error){$('user-list').innerHTML=`<div class="notice">${esc(error.message)}</div>`}}async function loadFinance(){loading('transaction-list',2);try{const txs=preview?[{kind:'usage',created_at:new Date(),side:'debit',amount_irr:125000}]:await api('/v1/webapp/transactions');$('transaction-list').innerHTML=txs.length?txs.map(x=>card(kindFa(x.kind),date(x.created_at),`<b class="amount ${x.side}">${x.side==='credit'?'+':'−'} ${money(x.amount_irr)}</b>`)).join(''):'<div class="notice">تراکنشی ثبت نشده است.</div>';if(perm('decide_funding'))await loadFunding();if(perm('decide_adjustment'))await loadApprovals()}catch(error){$('transaction-list').innerHTML=`<div class="notice">${esc(error.message)}</div>`}}function kindFa(k){return({usage:'تسویه مصرف',fund:'شارژ کیف پول',adjustment:'اصلاح حساب'})[k]||k}
const FUNDING_PATH=()=>state.caps.is_system_admin?'/v1/webapp/admin/funding-requests':'/v1/webapp/funding-requests';
async function loadFunding(){const rows=preview?[{id:'1',organization:'نماینده شمال',telegram_id:123,amount_irr:10000000,created_at:new Date()}]:await api(FUNDING_PATH());$('pending-count').textContent=`${new Intl.NumberFormat('fa-IR').format(rows.length)} درخواست`;$('funding-list').innerHTML=rows.length?rows.map(x=>card(x.organization,`${date(x.created_at)} · ${esc(x.telegram_id)}`,`<b>${money(x.amount_irr)}</b><button class="ghost" data-fund="${x.id}" data-decision="approve">تأیید</button><button class="ghost danger" data-fund="${x.id}" data-decision="reject">رد</button>`)).join(''):'<div class="notice">درخواست بازی وجود ندارد.</div>';qa('[data-fund]').forEach(btn=>btn.onclick=()=>decideFunding(btn))}
async function decideFunding(btn){btn.disabled=true;try{await api(`${FUNDING_PATH()}/${btn.dataset.fund}/${btn.dataset.decision}`,{method:'POST'});toast(btn.dataset.decision==='approve'?'کیف پول شارژ شد':'درخواست رد شد');await Promise.all([loadFunding(),loadDashboard()])}catch(error){toast(error.message,true)}finally{btn.disabled=false}}
const APPROVAL_ACTION={['wallet.adjust']:'اصلاح کیف پول'};
async function loadApprovals(){const rows=preview?[{id:'a1',action:'wallet.adjust',target_id:'org',payload:{amount_irr:-500000,note:'تجاوز ثبت‌شده در صورتحساب'},created_at:new Date()}]:await api('/v1/webapp/admin/approvals');$('approval-count').textContent=`${new Intl.NumberFormat('fa-IR').format(rows.length)} در انتظار`;$('approval-list').innerHTML=rows.length?rows.map(a=>card(`${APPROVAL_ACTION[a.action]||a.action} · ${esc(a.target_id)}`,`${date(a.created_at)} · ${esc(String(a.payload?.note||'').slice(0,120))}`,`<b class="amount ${Number(a.payload?.amount_irr)>0?'credit':'debit'}">${money(a.payload?.amount_irr)}</b><button class="ghost" data-approval="${a.id}" data-decision="approve">تأیید</button><button class="ghost danger" data-approval="${a.id}" data-decision="reject">رد</button>`)).join(''):'<div class="notice">درخواست تأییدی برای بررسی نمانده است.</div>';qa('[data-approval]').forEach(btn=>btn.onclick=async()=>{btn.disabled=true;try{await api(`/v1/webapp/admin/approvals/${btn.dataset.approval}/${btn.dataset.decision}`,{method:'POST'});toast('تصمیم ثبت شد');await Promise.all([loadApprovals(),loadDashboard()])}catch(error){toast(error.message,true)}finally{btn.disabled=false}})}
async function loadAudit(){loading('audit-list',2);try{const rows=preview?[{action:'panel.create',organization:'نمایندگی مرکزی',actor:'مدیر سیستم',created_at:new Date()}]:await api('/v1/webapp/audit?limit=50');$('audit-panel').classList.remove('hidden');$('audit-list').innerHTML=rows.length?rows.map(x=>card(`${esc(x.action)} · ${esc(x.organization||'—')}`,`${date(x.created_at)} · ${esc(x.actor||'سیستم')} · ${esc(x.target_type)} ${esc(x.target_id||'')}`)).join(''):'<div class="notice">گزارشی ثبت نشده است.</div>'}catch(error){$('audit-list').innerHTML=`<div class="notice">${esc(error.message)}</div>`}}function renderProfile(){if(!state.dashboard)return;const d=state.dashboard;$('profile-org').textContent=d.organization.name;$('profile-role').textContent=state.caps.is_system_admin?'مدیر کل سیستم':roleFa(state.caps.role);$('profile-id').textContent=state.caps.is_system_admin?'دسترسی سازمانی':'حساب نماینده'}function organizationOptions(){const rows=[{id:state.caps.organization_id,name:'کیف پول خودتان'}].concat(state.resellers.map(x=>({id:x.id,name:x.name})));return `<label>سازمان هدف<select name="organization_id" required>${rows.map(x=>`<option value="${esc(x.id)}">${esc(x.name)}</option>`).join('')}</select></label>`}
function openModal(type){state.mode=type;$('modal').classList.remove('hidden');$('modal-error').textContent='';let title='',kicker='',fields='';if(type==='fund'){title='درخواست افزایش اعتبار';kicker='کیف پول';fields='<label>مبلغ درخواستی به ریال<input name="amount_irr" type="number" min="1" required placeholder="10000000"></label>'}else if(type==='adjust'){title='درخواست اصلاح حساب';kicker='حسابداری';fields=organizationOptions()+'<label>مبلغ به ریال (منفی برای بستانکاری)<input name="amount_irr" type="number" required placeholder="-500000"><small>مبلغ منفی فقط تا سقف اعتبار توافق‌شده پذیرفته می‌شود.</small></label><label>توضیح عملیات<textarea name="note" minlength="10" maxlength="2000" required rows="3" placeholder="دلیل اصلاح، شماره فاکتور یا مرجع حسابداری"></textarea></label>'}else if(type==='limit'){title='سقف مصرف پنل';kicker='زیرساخت';fields=`<label>سقف به بایت<input name="limit_use_in_bytes" type="number" min="0" required placeholder="1099511627776"><small>برای سرور ${esc(state.panelName||'انتخاب‌شده')} و Admin صورتحساب آن اعمال می‌شود.</small></label>`}else if(type==='coefficient'){title='ضریب صورتحساب';kicker='قیمت‌گذاری';fields='<label>ضریب مصرف<input name="usage_coefficient" type="number" step="0.01" min="0.01" max="1000" required value="1"><small>مصرف ثبت‌شده پنل در این ضریب ضرب می‌شود؛ فقط مدیر کل سیستم می‌تواند آن را تغییر دهد.</small></label>'}else if(type==='reseller'){title=state.caps.is_system_admin?'ساخت نماینده جدید':'ساخت زیرنماینده';kicker='شبکه فروش';fields='<label>نام کسب‌وکار<input name="name" required></label><label>شناسه انگلیسی<input name="slug" required pattern="[a-z0-9][a-z0-9-]{1,78}[a-z0-9]"></label>'+(state.caps.is_system_admin?'<label>شناسه عددی تلگرام<input name="telegram_id" type="number" min="1" required><small>کاربر می‌تواند با دستور /id شناسه خود را دریافت کند.</small></label>':'')+'<label>قیمت هر گیگابایت (ریال)<input name="price_per_gib_irr" type="number" min="1" required></label><label>سقف اعتبار اولیه<input name="credit_limit_irr" type="number" min="0" value="0"></label>'}else{title='ثبت سرور پاسارگارد';kicker='زیرساخت';fields='<label>نام سرور<input name="name" required></label><label>آدرس HTTPS پنل<input name="base_url" type="url" required placeholder="https://panel.example.com"></label><label>API Key<input name="api_key" type="password" minlength="8" required></label><label>نام کاربری مالک (اختیاری)<input name="owner_username"></label><label>رمز مالک (اختیاری)<input name="owner_password" type="password"></label><label>Admin ID برای صورتحساب (اختیاری)<input name="pg_admin_id" type="number" min="1"></label><label>نام Admin (اختیاری)<input name="admin_username"></label>'+(state.caps.is_system_admin?'<label class="check"><input type="checkbox" id="verify-tls" name="verify_tls" checked> اعتبارسنجی گواهی TLS</label><p id="tls-warning" class="error hidden">با خاموش‌کردن اعتبارسنجی، ارتباط با این سرور در برابر شنود و جعل محافظت نمی‌شود؛ آن را فقط برای گواهی self-signed آزمایشگاهی بردارید.</p>':'')}
$('modal-title').textContent=title;$('modal-kicker').textContent=kicker;$('modal-fields').innerHTML=fields;const tls=$('verify-tls');if(tls)tls.onchange=()=>$('tls-warning').classList.toggle('hidden',tls.checked)}$('modal-form').onsubmit=async event=>{event.preventDefault();const button=$('modal-submit'),data=Object.fromEntries(new FormData(event.currentTarget));button.disabled=true;try{if(state.mode==='fund')await api('/v1/webapp/funding-requests',{method:'POST',body:JSON.stringify({amount_irr:+data.amount_irr})});else if(state.mode==='adjust')await api('/v1/webapp/admin/adjustments',{method:'POST',body:JSON.stringify({organization_id:data.organization_id,amount_irr:+data.amount_irr,note:data.note})});else if(state.mode==='limit')await api(`/v1/webapp/panels/${state.panelId}/limit`,{method:'POST',body:JSON.stringify({limit_use_in_bytes:+data.limit_use_in_bytes})});else if(state.mode==='coefficient')await api(`/v1/webapp/panels/${state.panelId}/coefficient`,{method:'POST',body:JSON.stringify({usage_coefficient:data.usage_coefficient})});else if(state.mode==='reseller'){const payload={...data,telegram_id:data.telegram_id?+data.telegram_id:undefined,price_per_gib_irr:+data.price_per_gib_irr,credit_limit_irr:+data.credit_limit_irr};await api(state.caps.is_system_admin?'/v1/webapp/admin/resellers':'/v1/webapp/children',{method:'POST',body:JSON.stringify(payload)})}else await api('/v1/webapp/panels',{method:'POST',body:JSON.stringify({...data,pg_admin_id:data.pg_admin_id?+data.pg_admin_id:null,verify_tls:'verify_tls'in data})});$('modal').classList.add('hidden');event.currentTarget.reset();toast(state.mode==='adjust'?'درخواست اصلاح ثبت و در انتظار تأیید است':'عملیات با موفقیت انجام شد');await loadDashboard();if(state.mode==='reseller')await loadResellers();if(state.mode==='server')await loadServers();if(state.mode==='fund'||state.mode==='adjust')await loadFinance()}catch(error){$('modal-error').textContent=error.message;tg?.HapticFeedback?.notificationOccurred('error')}finally{button.disabled=false}};$('setup-form').onsubmit=async event=>{event.preventDefault();try{await api('/v1/onboarding/bootstrap',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))});$('setup').classList.add('hidden');toast('فضای کاری ساخته شد');await boot()}catch(error){$('setup-error').textContent=error.message}};$('cancel').onclick=()=>$('modal').classList.add('hidden');$('close-nodes').onclick=()=>$('node-panel').classList.add('hidden');$('close-users').onclick=()=>$('user-panel').classList.add('hidden');$('users-prev').onclick=()=>{if(state.userOffset>0){state.userOffset=Math.max(0,state.userOffset-20);loadUsers()}};$('users-next').onclick=()=>{state.userOffset+=20;loadUsers()};$('refresh').onclick=async()=>{try{await loadDashboard();const active=q('.page.active')?.id;if(active==='network')await loadResellers();if(active==='servers')await loadServers();if(active==='finance')await loadFinance();toast('اطلاعات به‌روز شد')}catch(error){toast(error.message,true)}};$('reload-finance').onclick=loadFinance;$('load-audit').onclick=loadAudit;$('close-audit').onclick=()=>$('audit-panel').classList.add('hidden');qa('[data-page]').forEach(btn=>btn.onclick=()=>setPage(btn.dataset.page));qa('[data-nav]').forEach(btn=>btn.onclick=()=>setPage(btn.dataset.nav));qa('[data-action]').forEach(btn=>btn.onclick=()=>openModal(btn.dataset.action));$('reseller-search').oninput=event=>{const term=event.target.value.trim().toLowerCase();renderResellers(state.resellers.filter(x=>[x.name,x.slug,x.telegram_id].some(v=>String(v||'').toLowerCase().includes(term))))};boot();function initMotion(){const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;document.addEventListener('pointerdown',event=>{if(reduce)return;const button=event.target.closest('button');if(!button)return;const rect=button.getBoundingClientRect(),ripple=document.createElement('span');ripple.className='ripple';ripple.style.left=`${event.clientX-rect.left}px`;ripple.style.top=`${event.clientY-rect.top}px`;button.appendChild(ripple);setTimeout(()=>ripple.remove(),700)});if(reduce||!window.matchMedia('(hover:hover) and (pointer:fine)').matches)return;qa('.hero-card,.quick-grid button').forEach(card=>{card.classList.add('tilt');card.addEventListener('pointermove',event=>{const r=card.getBoundingClientRect(),x=(event.clientX-r.left)/r.width-.5,y=(event.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*3}deg) rotateY(${x*4}deg) translateY(-2px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}initMotion();
# Security

این پروژه در مسیر مالی و مدیریتی حساس قرار دارد. آسیب‌پذیری‌ها را عمومی ثبت نکنید؛ آن‌ها را به مالک Repository گزارش دهید.

## رازها

- `APP_ENCRYPTION_KEY` با Fernet، کلید API و رمز مالک پنل را به شکل `enc://…` در دیتابیس ذخیره می‌کند؛
  مقدار `env://NAME` اشاره به متغیر محیطی است و هیچ‌وقت خودِ مقدار.
- هیچ endpointای راز را برنمی‌گرداند و هیچ لاگی آن را چاپ نمی‌کند؛ تست‌ها این مرز را می‌سنجند.
- کلید API و رمز پنل فقط در WebApp دریافت می‌شوند، هرگز در چت تلگرام.
- `.env` با mode ۶۰۰ نوشته می‌شود، در `.gitignore` است و `release-check.sh` هر فایل ردیابی‌شده را
  روی الگوی کلید خصوصی، `AKIA`، `ghp_` و `xox` اسکن می‌کند.
- `CONTROL_API_KEY` و کلیدهای پاسارگارد را دوره‌ای Rotate کنید.

## هویت و دسترسی

- درخواست‌های WebApp با امضای HMAC تلگرام (`initData`) و اعتبار زمانی `auth_date` بررسی می‌شوند؛
  مقایسه با `hmac.compare_digest` انجام می‌شود.
- نقش از ردیف عضویت فعال در سرور خوانده می‌شود؛ `ROOT_TELEGRAM_ID` تنها شناسه‌ای است که
  `system_admin` می‌شود و تنها همان شناسه می‌تواند فضای کاری را bootstrap کند (یک‌بار).
- هر مسیر، مجوز متناظر در `rbac.MATRIX` را دوباره بررسی می‌کند؛ پنهان‌کردن دکمه در مرورگر
  یک تصمیم امنیتی محسوب نمی‌شود.
- اصلاح حساب نیاز به دو نفر دارد: درخواست‌کننده و تأییدکننده نمی‌توانند یک نفر باشند.
- `/metrics` تا تنظیم‌نشدن `METRICS_TOKEN` بسته است و توکن آن با مقایسهٔ زمان‌ثابت بررسی می‌شود.

## ورودی و ترافیک

- همهٔ بدنه‌ها با pydantic (`extra="forbid"`) اعتبارسنجی می‌شوند؛ آدرس پنل باید `https` باشد،
  hostname داشته باشد و هیچ credentialای در آن مخفی نباشد.
- هیچ SQL با الحاق رشته‌ای ساخته نمی‌شود؛ همه‌جا پارامتر کوئری است.
- محدودساز نرخ روی نوشتن‌های احرازهویت‌نشده و نقطهٔ ورود تلگرام اعمال می‌شود و `429` برمی‌گرداند.
- TLS پنل به‌صورت پیش‌فرض تأیید می‌شود. برداشتن اعتبارسنجی فقط از فرم WebApp و فقط برای مدیر کل
  سیستم ممکن است، با هشدار در همان فرم و ثبت در رسیدگی (`panel.create` با `verify_tls: false`).
- هدرهای امنیتی (nosniff، DENY، HSTS در gateway، CSP frame-ancestors) در `main.py` و پیکربندی
  `deploy/` اعمال می‌شوند.

## عملیات

- `scripts/doctor.sh` سلامت سرویس‌ها، باز بودن `/metrics`، drift دفتر، صف outbox و تأخیر استریم تلگرام را بررسی می‌کند.
- `scripts/backup.sh` پیش از پذیرش archive محتوای dump را بررسی و نسخه‌های قدیمی را طبق `BACKUP_RETENTION` می‌چرخاند.
- `scripts/restore.sh` به‌صورت پیش‌فرض در دیتابیس scratch (`control_restore`) بازیابی می‌کند و
  جایگزینی دیتابیس زنده فقط با `--into control --force` و پس از یک backup ایمنی انجام می‌گیرد.
- دسترسی SSH و PostgreSQL را عمومی نکنید؛ پیش از Production تست بازیابی Backup و تست بار را انجام دهید.

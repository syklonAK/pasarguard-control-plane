# Security

این پروژه در مسیر مالی و مدیریتی حساس قرار دارد. آسیب‌پذیری‌ها را عمومی ثبت نکنید؛ آن‌ها را به مالک Repository گزارش دهید.

- هیچ Token، رمز، `.env` یا Backup را Commit نکنید.
- `CONTROL_API_KEY` و کلیدهای پاسارگارد را دوره‌ای Rotate کنید.
- دسترسی SSH و PostgreSQL را عمومی نکنید.
- پیش از Production، تست بازیابی Backup و تست بار انجام دهید.

# مشارکت

پیش از ثبت Change، این مخزن را با چشم یک سیستم مالی ببینید: یک اشتباه در دفتر، یک صورتحساب
تکراری یا یک دسترسی بازِ بیش‌ازحد، مستقیماً پول و امنیت نمایندگان را می‌سوزاند.

## مسیر کار

1. مخزن را clone کنید و وابستگی‌ها را نصب کنید: `pip install -e .[test]`
2. تغییر را در کوچک‌ترین برش قابل‌تست بدهید؛ migration باید idempotent باشد و دادهٔ موجود را حذف نکند
3. اگر رفتار دسترسی عوض می‌شود، `src/control_plane/rbac.py` تنها جای تعریف نقش است؛ هم ربات و هم
   وب‌اپ از همان `capabilities` خوانده و هر route سمت سرور دوباره بررسی می‌کند
4. اگر صورت‌حساب عوض می‌شود، هیچ نوشتنی به موجودی جز از مسیر `Transaction` مجاز نیست
   (تریگر دیتابیس آن را رد می‌کند) و ناعدی را با view `ledger_imbalance` بسنجید
5. تست بنویسید، سپس اجرا کنید:

```bash
pytest                       # علیه PostgreSQL واقعی؛ بدون تست پاس‌شده کامیت نهایی نیست
bash scripts/release-check.sh
```

## خط قرمزها

- `.env` و رازها را بازنویسی یا commit نکنید؛ کلید API و رمز پنل هرگز در چت تلگرام دریافت نمی‌شود
- راز را در response یا log برنگردانید و در تست‌ها همین مرز را بسنجید
- ثبت سرور PasarGuard را به سؤالات ترمینال منتقل نکنید؛ مسیر آن وب‌اپ است
- دادهٔ موجود را حذف نکنید؛ اگر واقعاً لازم است، در OpenAPI/PR و با مسیر بازگشت مشخص اعلام کنید
- به‌جای ارائهٔ Mock بدون اتصال Backend، رفتار را به سرویس واقعی وصل کنید

## گزارش و درخواست

برای ایراد و قابلیت از قالب‌های `.github/ISSUE_TEMPLATE` استفاده کنید (ثبت issue خالی غیرفعال است).
آسیب‌پذیری امنیتی را خصوصی گزارش دهید: مسیر آن در `SECURITY.md` و `.github/ISSUE_TEMPLATE/config.yml`
آمده است.
